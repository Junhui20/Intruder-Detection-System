# Optional extras — both only feed core/animal_recognition.py, which runs
# without them (pet identification stays colour-only until the re-ID work lands).
#
#   face-recognition  builds dlib from source (cmake + C++ toolchain). Last
#                     release February 2020.
#   mediapipe         imported when present; never required.
#
#   pip install -r requirements-optional.txt

face-recognition>=1.3.0,<2
mediapipe>=0.10.14,<1
